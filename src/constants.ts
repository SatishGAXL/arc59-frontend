// URL for the Algorand Indexer
export const indexerUrl = "https://testnet-idx.algonode.cloud";
// Token for the Algorand Indexer (dummy value, replace with actual token if needed)
export const indexerToken = "a".repeat(64);
// Port for the Algorand Indexer
export const indexerPort = 443;
